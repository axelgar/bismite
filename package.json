{
  "name": "bismite-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --import tsx --test packages/sdk/test/*.test.ts services/counter/test/*.test.ts examples/nextjs-chat/test/*.test.ts",
    "smoke": "node scripts/smoke.ts",
    "build": "pnpm --filter bismite build",
    "counter": "tsx services/counter/src/index.ts",
    "issue-key": "node scripts/issue-key.ts",
    "example": "pnpm --filter nextjs-chat dev"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
